Sources for the plugin `plugin.applovinMax.Chartboost`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.applovinMax.Chartboost" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
